name: Legacy Cursor Controller (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Runtime orchestration lives only in the default-branch GitHub-Centric Cursor Runner."
